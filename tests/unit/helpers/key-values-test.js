
import { keyValues } from 'biscuitman/helpers/key-values';
import { module, test } from 'qunit';

module('Unit | Helper | key values');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keyValues([42]);
  assert.ok(result);
});

