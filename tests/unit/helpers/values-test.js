
import { values } from 'biscuitman/helpers/values';
import { module, test } from 'qunit';

module('Unit | Helper | values');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = values([42]);
  assert.ok(result);
});

